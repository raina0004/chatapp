import * as React from 'react';

const Group = () => {
    // Define an array of names
    const names = ["Deepak", "Rahul", "Shiva", "Priti", "Sandeep", "Sushmita"];

    return names; // Return the array of names
}

export default Group;
